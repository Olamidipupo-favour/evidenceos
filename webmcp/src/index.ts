/**
 * EvidenceOS WebMCP tool layer — shared contract types and registration
 * helpers. Tools themselves are wired to app features as they land.
 */

export * from "./context.js";
export * from "./types.js";
