/**
 * EvidenceOS WebMCP tool layer — shared tool contract types, schemas and
 * registration helpers. `execute` handlers live in the frontend
 * (`frontend/src/lib/webmcp/`), where they reuse the same API client as the
 * human UI.
 */

export * from "./context.js";
export * from "./tools.js";
export * from "./types.js";
