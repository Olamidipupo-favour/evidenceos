# EvidenceOS frontend — environment configuration
# Copy to frontend/.env.local (or frontend/.env in dev) and adjust as needed.
# Never commit real secrets. Vars prefixed NEXT_PUBLIC_ are inlined at build time.

# Base URL of the EvidenceOS backend API.
NEXT_PUBLIC_API_URL=http://localhost:8000