# EvidenceOS frontend — environment configuration
# Copy to frontend/.env.local and adjust as needed. Never commit real secrets.

# Base URL of the deployed EvidenceOS API. This is inlined at build time, so
# set it in the deploy platform BEFORE building (e.g. Vercel project Env Vars).
NEXT_PUBLIC_API_URL=http://localhost:8000